export const metadata = {
  title: "Dashboard — BharatPulse AI",
  description: "Alerts, charts, and AI recommendations for your outlets.",
};

export default function DashboardLayout({ children }) {
  return children;
}
